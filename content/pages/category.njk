---
layout: base.njk
title: Category
pagination:
  data: collections.categories
  size: 1
  alias: category
permalink: "/category/{{ category }}/"
---

<h1>Category: {{ category | displayName }}</h1>

<ul>
  {% for post in collections.posts | filterByCategory(category) | reverse %}
    <li><a href="{{ post.url }}">{{ post.data.title }}</a> <small>({{ post.date | readableDate }})</small></li>
  {% endfor %}
</ul>
